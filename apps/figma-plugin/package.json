{
  "name": "@typefigma/figma-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Native Figma plugin for TypeFigma — extract variables, styles, auto-layout and export to WordPress pipeline",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist/"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.108.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
